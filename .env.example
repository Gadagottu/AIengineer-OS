# API Configuration
# Switch between local and remote server by changing VITE_API_BASE_URL

# Local server (via Vite proxy)
VITE_API_BASE_URL=/api

# Remote ngrok server (uncomment and add your ngrok URL)
# VITE_API_BASE_URL=https://your-ngrok-url.ngrok-free.dev

API_TARGET=http://127.0.0.1:8000
